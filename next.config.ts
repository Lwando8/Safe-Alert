/**
 * Root Next config for Vercel when project Root Directory is unset (monorepo).
 * Local Expo tooling ignores this file. Prefer setting Vercel Root Directory to
 * `apps/web` in the dashboard when possible.
 */
export { default } from "./apps/web/next.config";
