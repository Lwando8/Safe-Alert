{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "npm install",
  "buildCommand": "npm run domain:build && npm run build --workspace=@seren/web && rm -rf .next && cp -a apps/web/.next .next && rm -rf public && cp -a apps/web/public public"
}
